<?xml version="1.0" encoding="utf-8"?>
<!-- res/drawable/border_green.xml -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- The outer circle (border) -->
    <item android:right="5dp" android:left="5dp" android:top="5dp" android:bottom="5dp">
        <shape android:shape="oval">
            <solid android:color="@android:color/transparent"/>
            <stroke android:width="10dp" android:color="#0B800B"/> <!-- Green border -->
        </shape>
    </item>

    <!-- The inner circle (actual image area) -->
    <item>
        <shape android:shape="oval">
            <solid android:color="@android:color/holo_green_dark"/> <!-- Placeholder color -->
        </shape>
    </item>
</layer-list>
